name: Active apps

on:
  push:
    branches: [main, pilot/supabase]
  pull_request:
    branches: [main]

concurrency:
  group: active-apps-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: Build + design guard
    runs-on: ubuntu-latest
    env:
      CI: true
      HUSKY: 0
      SUPABASE_URL: http://127.0.0.1:54321
      SUPABASE_ANON_KEY: local-ci-placeholder
      SITE_PUBLIC_URL: https://dukarun.com
      APP_PUBLIC_URL: https://app.dukarun.com
      STOREFRONT_PUBLIC_URL: https://store.dukarun.com
      MARKETING_VIDEO_BASE_URL: https://cdn.dukarun.com/video
      PUBLIC_DATA_MODE: fixture

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22.23.1'
          cache: npm
          cache-dependency-path: package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Enforce The Counter design system
        run: npm run design-guard -w @dukarun/web

      - name: Enforce offline data safety
        run: npm run offline-safety-guard -w @dukarun/web

      - name: Enforce public design rules
        run: npm run public-design-guard

      - name: Validate video compositions
        run: npm run check:video

      - name: Build active Angular applications
        run: npm run build:active
