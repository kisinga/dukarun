// Stock/shift UI components shared across features.
// Secondary entry point (@dukarun/stock/components) so that importing
// components does not pull page code into the eager services barrel.
export { RecordExpenseModalComponent } from './record-expense-modal.component';
