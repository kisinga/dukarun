import {
  heroArrowLeft,
  heroArrowTrendingUp,
  heroBanknotes,
  heroCalendar,
  heroCheck,
  heroCheckBadge,
  heroChevronDown,
  heroClipboardDocumentList,
  heroClock,
  heroCreditCard,
  heroDocumentText,
  heroEnvelope,
  heroEye,
  heroHashtag,
  heroInformationCircle,
  heroLockClosed,
  heroMapPin,
  heroPencilSquare,
  heroPhone,
  heroPlus,
  heroShoppingBag,
  heroTag,
  heroTrash,
  heroUser,
  heroUsers,
  heroWallet,
  heroXMark,
} from '@ng-icons/heroicons/outline';

/**
 * The app's registered icon set — single source of truth.
 *
 * Icons come from @ng-icons/heroicons (outline). Register once via
 * `provideIcons(APP_ICONS)` in app.config, then reference by key anywhere:
 *   `<ng-icon name="heroWallet" />`
 *
 * Add an icon by importing its `hero*` export and listing it here. ESM
 * tree-shakes, so only referenced icons are bundled — keep this to icons
 * the app actually renders.
 */
export const APP_ICONS = {
  heroArrowLeft,
  heroArrowTrendingUp,
  heroBanknotes,
  heroCalendar,
  heroCheck,
  heroCheckBadge,
  heroChevronDown,
  heroClipboardDocumentList,
  heroClock,
  heroCreditCard,
  heroDocumentText,
  heroEnvelope,
  heroEye,
  heroHashtag,
  heroInformationCircle,
  heroLockClosed,
  heroMapPin,
  heroPencilSquare,
  heroPhone,
  heroPlus,
  heroShoppingBag,
  heroTag,
  heroTrash,
  heroUser,
  heroUsers,
  heroWallet,
  heroXMark,
};
